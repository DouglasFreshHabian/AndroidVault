# AndroidVault
Android Vault is a purpose-built Android lab application designed to demonstrate how the `android:debuggable` flag impacts Android’s sandbox enforcement and the behavior of the `run-as` command. ⭐️
